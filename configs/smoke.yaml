model_type: hcr_moment
dataset: tiny_shakespeare
output_dir: runs/smoke
context_length: 32
batch_size: 4
d_model: 32
n_layers: 1
n_heads: 4
mlp_ratio: 2
dropout: 0.0
learning_rate: 0.001
weight_decay: 0.0
max_steps: 5
eval_interval: 5
eval_batches: 1
log_interval: 1
grad_clip: 1.0
task: causal
sample_at_end: false

